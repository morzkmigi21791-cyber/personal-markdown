## Что сделал
Начал работу с доменами пользователей. Для этого используется nginx, он принимает запросы пользователей и отправляет, некий посредник между сервером и пользователем. Сейчас функционал реализован не полностью, но он уже работает и запускается в docker. Теперь проект полностью запускается при помощи 1 файла, при этом проверяет и создаёт контейнеры и images в докере, смотрит есть ли используемые порты, и перезапускает

## Что сделаю завтра:
Доработаю функционал хоста сайтов